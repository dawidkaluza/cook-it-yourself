apiVersion: v1
kind: Service
metadata:
  name: user-service-redis
  labels:
    app: user-service-redis
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: user-service-redis
  clusterIP: None
