name: Kitchen-service CI

on:
  push:
    branches: [ "master" ]
    paths: [ 'kitchen-service/**', '.github/workflows/kitchen-service-ci.yml' ]
  pull_request:
    branches: [ "master" ]
    paths: [ 'kitchen-service/**', '.github/workflows/kitchen-service-ci.yml' ]

jobs:
  build:
    name: 'Build and test'
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      actions: read
      checks: write
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@417ae3ccd767c252f5661f1ace9f835f9654f2b5 # v3.1.0

      - name: Build and test
        working-directory: ./kitchen-service
        run: ./gradlew build

      - name: Store test results
        if: ${{ success() || failure() }}
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            kitchen-service/build/test-results/
            kitchen-service/build/reports/

      - name: Report test results
        if: ${{ success() || failure() }}
        uses: dorny/test-reporter@v1
        with:
          name: Test reports
          path: 'kitchen-service/build/test-results/**/*.xml'
          reporter: java-junit